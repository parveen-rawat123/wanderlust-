<% layout("/layouts/boilerplate")%>
<body>
     <h3>listing details :</h3>
     <ul>
        <li> <%= listinginfo.title %></li>
        <li> <%= listinginfo.description %></li>
        <li>  &#8377;<%= listinginfo.price.toLocaleString("en-IN") %></li>
        <li> <%= listinginfo.location %></li>
        <li> <%= listinginfo.country %></li>
     </ul>
     <br>
     <a href="/listings/<%= listinginfo._id %>/edit">edit this listing</a>

    <form action="/listings/<%= listinginfo._id%>?_method=DELETE" method="post">
        <button>delete this listing</button>
    </form>
</body>